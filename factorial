//faktoriyel hesaplama
#include <stdio.h>

int main() {
  int number,i,factorial;
  factorial=1;
  
  printf("enter a number:");
  scanf("%d",&number);
  
  for(i=1;i<=number;i++){
      factorial*=i;
   
  }

 printf("%d",factorial);
    return 0;
}
